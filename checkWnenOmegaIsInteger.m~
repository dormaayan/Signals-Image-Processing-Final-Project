for i=1:1:10,
 B = 12379;
 A = rand * 100;
 phi = rand*2*pi;
 alpha = rand * 10;
 for j=10:10:100,
    omega = rand * 10;
     [b,N,] = func3Numerically( omega,phi,j,alpha,B);
    if abs(b-b_org)>0.001
        disp('Dependent !!!');
    end 
 end
 fprintf('done: %d \n',i);
end

